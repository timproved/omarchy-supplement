This directory ~/omarchy-supplement/ is my central place for overriding the omarchy defaults, as well as
extending the defaults in a central place, so that I can apply them to a new machine by just running the install script.

